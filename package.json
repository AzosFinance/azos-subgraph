{
  "name": "magenta",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ magenta",
    "create-local": "graph create --node http://localhost:8020/ magenta",
    "remove-local": "graph remove --node http://localhost:8020/ magenta",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 magenta",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.51.1",
    "@graphprotocol/graph-ts": "0.30.0",
    "@openzeppelin/subgraphs": "^0.1.8-5"
  },
  "devDependencies": {
    "matchstick-as": "0.5.0"
  }
}
